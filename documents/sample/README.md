# Sample corpus

Three short, made-up documents so `homebrew-rag ingest` and the example golden
set have something to run against on a fresh clone. Replace them with your own
material — anything you drop directly in `documents/` is gitignored.
